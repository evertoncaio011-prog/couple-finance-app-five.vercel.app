# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables — ignora QUALQUER .env (inclusive .env puro, que
# antes escapava dessa regra), menos o .env.example que é só um molde.
.env
.env.*
!.env.example

# Common ignores
node_modules
.next/
.DS_Store
.vercel
# Nunca versionar zips (evita commits gigantes por engano)
*.zip
*.tar.gz
*.rar

# Metadados do Windows Explorer
desktop.ini
.vercel
